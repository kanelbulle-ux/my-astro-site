---
---

<section
  class="relative min-h-screen flex items-center justify-center bg-gradient-to-b from-secure-bg to-secure-light"
>
  <div class="container mx-auto px-6 text-center max-w-4xl pt-24 md:pt-20">
    <div class="animate-fade-in-up">
      <h1
        class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-secure-black leading-tighter tracking-tight mb-6 md:mb-8"
      >
        Secure your WordPress sites <span class="block">smarter.</span>
      </h1>

      <p
        class="text-lg md:text-xl text-secure-gray leading-relaxed mb-12 max-w-3xl mx-auto"
      >
        SecureList is the only centralized IP allowlist platform offering
        real-time protection and instant access control to boost security,
        reduce breaches, and eliminate unauthorized access.
      </p>

      <button
        id="hero-cta"
        class="group bg-secure-black text-white px-8 py-4 rounded-xl font-medium text-lg hover:translate-y-[-2px] hover:shadow-xl transition-all duration-300 inline-flex items-center space-x-2"
      >
        <span>Try for Free</span>
        <svg
          class="w-5 h-5 group-hover:translate-x-1 transition-transform duration-200"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M9 5l7 7-7 7"></path>
        </svg>
      </button>
    </div>
  </div>
</section>

<script>
  const heroCta = document.getElementById("hero-cta");
  if (heroCta) {
    heroCta.addEventListener("click", () => {
      const demoSection = document.getElementById("demo");
      if (demoSection) {
        demoSection.scrollIntoView({ behavior: "smooth" });
      }
    });
  }
</script>
